# ManBoyx

Ma page personnelle : https://manboyx.github.io

Un seul fichier HTML, une feuille de style et un petit script (le nom en gros pixels). Aucune bibliothèque, aucun traceur, aucune requête vers un autre site.

Pour ajouter un lien, copiez un bloc `<li>…</li>` dans la liste `.links` de `index.html`.
